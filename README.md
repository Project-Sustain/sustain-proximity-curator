# sustain-proximity-curator
Curates a dataset outlining the proximity from set geospatial points, to other datasets.

### Usage:
`npm i`
Edit `config.json` to fit your needs, everything in there should be self explanatory.
Execute `./run.sh`
